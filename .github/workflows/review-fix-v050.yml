name: Apply v0.5 Review Fixes

on:
  push:
    branches:
      - feat/v050-network-compatibility
    paths:
      - '.github/scripts/review_fix_v050.py'
      - '.github/scripts/review_fix_v050_runner.py'
      - '.github/workflows/review-fix-v050.yml'

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: windows-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v7
        with:
          ref: feat/v050-network-compatibility
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Apply review fixes
        run: python .github/scripts/review_fix_v050_runner.py

      - name: Install test dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Compile
        run: python -m compileall -q .

      - name: Run tests
        run: python -m pytest -q

      - name: Commit reviewed fixes
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backup_manager.py codex_toolkit_gui.py config_manager.py network_diagnostics.py proxy.py proxy_discovery.py runtime_stats.py startup_manager.py transport_policy.py tests/test_network_diagnostics.py tests/test_v050_review_fixes.py
          if (git diff --cached --quiet) {
            Write-Host "No source changes to commit."
            exit 0
          }
          git commit -m "fix(v0.5.0): harden network recovery behavior after review"
          git push origin HEAD:feat/v050-network-compatibility
